# Session 19–21 September 2026 (the weekend)
One continuous session (context compacted once). Entries D15–D38. States 2–8 written or rewritten; all pass `scripts/state_lint.py`.

**What happened, in order.** State 2's figure replaced (recipe born: one plot type, expectation stated, graded verdict) → the lead's plain-language rule after three states drifted into shorthand (CLAUDE.md block + lint + pre-commit) → round 1 read (null; pipeline ≠ reference) → round 2, the lead's sub-category design (works; distance predicts without labels; seed floor) → all twelve categories → the "as measured" plots explained (pair-distance confound) → absolute vs relative (relative; 92 % level) → the regime ladder incl. from scratch (level share 85 → 45 %; nothing absolute replaces it) → the 69-measure search (bbox ties within; only DINOv2 orders across; nearest-few > coverage) → transfer to MOCHI → the continuum plot → training-object anchor trials queued → states 6–8, STATE.md, HANDOFF lessons.

**What the lead corrected.** Titles and deks in shorthand (three times); a figure that mixed in a measure that did not yet exist at that state; a concern presented as a kill; "status in beats"; a bar chart of coefficients as evidence; over-aggressive binning; a six-panel figure; an artifact claim overstated; my mis-reading of "results are in" (they were, unread); and — the one that mattered most — my first experimental design, replaced by his calibration-first design.

**What the agent got wrong on its own.** The knockout dose; MOCHI as a test set; the naive own-minus-others statistic (row effect); a mis-typed Hydra override; the `vit_large*` glob; a job-ID extractor that matched the SLURM banner; a search written as Python loops (hours) instead of matrix products (minutes); a first pass of the one-axis figure that hid the category step.

**Open when the session ended.** Anchor trials scoring; rung 3b to fold in; states 7–8 unreviewed; repos not yet on GitHub; demo/viewer rebuild pending the review.
